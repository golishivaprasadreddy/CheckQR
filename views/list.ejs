<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Saved QR Codes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-6 bg-gray-900">
    <h1 class="text-2xl font-bold text-white">Saved QR Codes</h1>
    <% if (qrList.length > 0) { %>
        <% qrList.forEach(qr => { %>
            <img src="<%= qr.qrCodeUrl %>" alt="QR Code" class="m-2">
        <% }); %>
    <% } else { %>
        <p>No QR Codes Found</p>
    <% } %>
</body>
</html>
