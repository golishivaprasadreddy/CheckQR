<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Saved QR Codes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="p-6 bg-gray-900">
    <h1 class="text-2xl font-bold text-white mb-4">Saved QR Codes</h1>
    <% if (qrList.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% qrList.forEach(qr => { %>
                <div class="bg-gray-800 p-4 rounded-lg shadow-md text-center">
                    <h2 class="text-lg font-bold text-white mb-2">Roll No: <%= qr.rollNo %></h2>
                    <img src="<%= qr.qrCodeUrl %>" alt="QR Code for <%= qr.rollNo %>" class="mx-auto">
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <p class="text-white">No QR Codes Found</p>
    <% } %>
</body>
</html>